public Plan createPlan() {
        return new Plan(
                project(),
                "Plan Name",
                "PLANKEY")
                .stages(
                        new Stage("Default Stage")
                        .jobs(
                                new Job(
                                        "Default Job",
                                        new BambooKey("JOB1")
                                )
                                .tasks(
                                        new ExampleTaskBuilder()
                                                .myText("Hello World! by Kapil")
                                )
                        )
                );
}
